# Head up 永久部署教程(5 分钟搞定)

> ## 为什么要部署?
> 沙箱临时域名(`webview.e2b.bj3...`)一停就打不开,数据全没。
> 部署到 **Vercel** 后:永久在线 + 自动 HTTPS + 苹果安卓都能直接打开 + 数据云端备份不丢失。
>
> ## 成本
> **完全免费**。Vercel 个人免费额度足够你一个人用(每月 100GB 流量、无限静态请求、100GB·小时 KV 存储)。

---

## 前置准备(只需 3 个账号,都免费)

| 账号 | 用途 | 注册地址 |
|------|------|----------|
| GitHub | 托管代码 + 触发 Vercel 自动部署 | https://github.com/signup |
| Vercel | 永久托管网站 + 后端 API + 数据库 | https://vercel.com/signup(可用 GitHub 一键登录) |

> 如果已有 GitHub 账号,只需再注册 Vercel,全程 3 分钟。

---

## 第一步:把项目文件上传到 GitHub

### 方法 A:网页上传(最简单,无需装任何软件)

1. 登录 GitHub,点右上角 **`+`** → **New repository**
2. 填写:
   - Repository name: `headup`(随便取)
   - 勾选 **Private**(私人仓库,别人看不到你的数据)
   - 点 **Create repository**
3. 在新仓库页面,点 **`uploading an existing file`**(上传已有文件)
4. 把 `vercel` 文件夹里的**所有文件**拖进去:
   ```
   index.html
   manifest.json
   sw.js
   icon-192.png
   icon-512.png
   vercel.json
   api/sync.js          ← 注意:这是后端,必须上传
   ```
   > ⚠️ `api` 是一个文件夹,上传时会保留结构。GitHub 网页上传支持拖入文件夹。
5. 点 **Commit changes**

### 方法 B:用命令行(如果你装了 git)

```bash
cd vercel
git init
git add .
git commit -m "headup 部署"
git branch -M main
git remote add origin https://github.com/你的用户名/headup.git
git push -u origin main
```

---

## 第二步:在 Vercel 导入项目

1. 登录 https://vercel.com(用 GitHub 账号一键登录)
2. 点 **`Add New...`** → **Project**
3. 在 "Import Git Repository" 列表里找到 `headup`,点 **Import**
4. 配置页面:
   - **Framework Preset**: 选 `Other`(或保持默认)
   - **Root Directory**: 保持 `./` 不变
   - **Build Command**: 留空(不填)
   - **Output Directory**: 留空(或填 `.`)
   - **Install Command**: 留空
5. 点 **Deploy** 🎉

等待约 30 秒,Vercel 会给你一个永久 URL,类似:
```
https://headup-xxxxx-你的用户名.vercel.app
```

> **这个 URL 永久有效**,苹果安卓任何浏览器打开都能用。每次你 push 代码到 GitHub,Vercel 会自动重新部署。

---

## 第三步:开启云端数据库(让数据永久不丢失)

> 没配数据库也能用,但数据只存在手机本地(localStorage)。换手机/清缓存会丢。
> 配了 Vercel KV 后,数据存云端,换设备也能同步。**强烈建议配**。

### 3.1 创建 KV 数据库

1. 进入 Vercel 项目面板 → 顶部 **Storage** 标签
2. 点 **Create Database** → 选 **KV** → 命名 `headup-db` → **Create**
3. 创建完成后,点 **Connect to Project** → 勾选你的 `headup` 项目 → **Connect**

### 3.2 添加 KV 依赖

Vercel KV 需要一个 npm 包。在你的 GitHub 仓库根目录新建一个文件 `package.json`,内容:

```json
{
  "dependencies": {
    "@vercel/kv": "^1.0.1"
  }
}
```

> 提交后 Vercel 会自动重新部署,装上这个包。

### 3.3 验证数据库生效

部署完成后,打开你的 Vercel URL,在设置里填一个"同步码"(比如 `myroom2026`)。
打开手机、平板、电脑,填**相同的同步码**,数据就会自动云端同步。

---

## 第四步:手机安装成 App(可选,体验更好)

### iPhone (Safari)
1. 用 Safari 打开你的 Vercel URL
2. 点底部 **分享** → **添加到主屏幕**
3. 桌面会多一个 "Head up" 图标,点开就是全屏 App

### Android (Chrome)
1. 用 Chrome 打开你的 Vercel URL
2. 浏览器会弹出 **"添加到主屏幕"** 提示(或菜单 → 添加到主屏幕)
3. 桌面会多一个图标,点开就是全屏 App

---

## 使用说明

### 数据存储机制(三层保护,绝不丢失)

| 层级 | 存哪 | 作用 |
|------|------|------|
| 第一层 | 手机本地 localStorage | 即使断网也能用,秒级读写 |
| 第二层 | Vercel 云端 KV | 换设备/清缓存后,输入同步码即可拉回全部数据 |
| 第三层 | 手动导出备份 | 设置页可随时导出 JSON 文件,万一云端出问题也能恢复 |

### 同步码怎么用

- 同步码 = 你的私密房间号
- **所有设备填同一个同步码**,数据自动双向同步
- 不同设备填不同同步码 = 各自独立,互不干扰
- 同步码只支持字母数字下划线短横线,1~40 位

### 离线时怎么办

- 顶部同步状态显示 **"离线·已存本地"**(灰色点)= 没网,但数据已存手机
- 联网后自动恢复同步,无需手动操作
- 下滑页面可手动触发刷新同步

---

## 常见问题

**Q: 部署后打不开?**
A: 等 1 分钟让 Vercel 完成首次部署。检查 GitHub 仓库里 `api/sync.js` 是否上传成功(很多人漏了这个文件夹)。

**Q: 数据没同步?**
A: ① 确认两台设备填了**相同**的同步码;② 确认 Vercel KV 已 Connect 到项目;③ 确认 `package.json` 里有 `@vercel/kv` 依赖。

**Q: 想改代码后更新?**
A: 改 GitHub 仓库的文件 → Vercel 自动重新部署 → 30 秒后生效。手机下拉刷新即可看到新版。

**Q: 免费额度够用吗?**
A: 个人用绰绰有余。KV 免费额度:300,000 次请求/月、256MB 存储。你一个人每天同步几百次,一个月也就几千次。

**Q: 会不会突然停服?**
A: Vercel 免费版不会主动停。只要你不主动删项目,URL 永久有效。即使免费额度用超,也只是限流,数据不丢。

---

## 文件清单(部署需要的)

```
headup/
├── index.html          # PWA 主程序(含全部前端逻辑)
├── manifest.json       # PWA 配置(图标、名称)
├── sw.js               # Service Worker(离线缓存)
├── icon-192.png        # 图标
├── icon-512.png        # 图标
├── vercel.json         # Vercel 部署配置
├── package.json        # 依赖声明(配 KV 用)
└── api/
    └── sync.js         # 后端同步 API(Vercel Serverless Function)
```

**这些文件都在 `vercel/` 文件夹里,直接上传即可。**
